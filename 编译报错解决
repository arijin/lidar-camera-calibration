1、ros_observer包报错：

```
stderr:ros_observer
lib/x86_64-linux-gnu/libpthread.so.0: 无法添加符号: DSO missing from command line
```

##### 解决：

对./autoware.ai/src/autoware/common/ros_observer中的CMakeLists.txt进行修改：

```
将所有的target_link_libraries(...)里都加入pthread
```

2、calibration_publisher包报错：

```
stderr:calibration_publisher
对cv::read(...).......未定义的引用
```

##### 解决：

对./autoware.ai/src/autoware/utilities/calibration_publisher中的CMakeLists.txt进行修改：

```
target_link_libraries里加入${OpenCV_LIBRARIES}，改成：
target_link_libraries(calibration_publisher
  ${catkin_LIBRARIES}
  ${OpenCV_LIBRARIES}
)
```

